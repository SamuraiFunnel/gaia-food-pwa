{
  "name": "gaia-food-app",
  "version": "1.0.0",
  "private": true,
  "description": "Gaia Food — app (UI statica + API) servita da server.js, zero dipendenze",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "test:watch": "node --test --watch",
    "test:coverage": "node --test --experimental-test-coverage"
  }
}
